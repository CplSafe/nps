# NPS系统安全审计报告

## 审计时间
2025-11-23

## 严重安全漏洞清单

### 🔴 严重漏洞 (Critical)

#### 1. 密码明文存储和传输
**位置**: `web/controllers/login.go:61`, `lib/file/obj.go:49-50`
**危害**: 
- 密码以明文形式存储在JSON文件中
- 数据库被入侵后所有用户密码暴露
- 内存dump可直接获取密码

**CVE相似度**: CVE-2021-xxxxx (明文密码存储)

#### 2. 使用弱加密算法 MD5
**位置**: `lib/crypt/crypt.go:60-64`, `web/controllers/base.go:36`
**危害**:
- MD5已被证明不安全，可通过彩虹表快速破解
- 认证机制使用MD5，容易被暴力破解
- 攻击者可以构造MD5碰撞进行认证绕过

**CVE相似度**: CWE-327 (使用不安全加密算法)

#### 3. AES加密使用密钥作为IV
**位置**: `lib/crypt/crypt.go:22`
**危害**:
- 使用密钥前16字节作为IV违反加密最佳实践
- 使得加密模式退化，降低安全性
- 相同明文会产生相同密文，泄露信息

**CVE相似度**: CWE-329 (未使用随机IV)

#### 4. 认证重放攻击
**位置**: `web/controllers/base.go:36`
**危害**:
- 即使缩短时间窗口，攻击者仍可在窗口内重放
- 可能导致未授权访问
- 需要使用一次性令牌(Nonce)彻底防止

**修复方案**: 使用Nonce（一次性令牌）机制
- 每个认证请求必须包含唯一的nonce
- 服务器记录已使用的nonce
- 相同nonce的请求会被拒绝
- 时间窗口设为5分钟（配合nonce使用）

**CVE相似度**: CWE-294 (认证绕过)

### 🟠 高危漏洞 (High)

#### 5. 路径遍历漏洞
**位置**: `server/proxy/http.go:192`, `server/proxy/http.go:208`
**危害**:
- 攻击者可以通过构造`../`路径访问任意文件
- 可能读取系统敏感文件如 `/etc/passwd`
- 缓存机制使用不安全的路径拼接

**攻击示例**:
```
GET http://host/../../../etc/passwd
```

**CVE相似度**: CWE-22 (路径遍历)

#### 6. 时序攻击漏洞
**位置**: `lib/common/util.go:71`, `web/controllers/login.go:61`
**危害**:
- 密码比对使用直接字符串比较 `==`
- 攻击者可通过测量响应时间逐字符破解密码
- 可大幅降低暴力破解难度

**CVE相似度**: CWE-208 (时序侧信道攻击)

#### 7. CSRF保护缺失
**位置**: 整个Web控制器系统
**危害**:
- 所有POST请求无CSRF Token验证
- 攻击者可诱导已登录用户执行恶意操作
- 可能导致账户被劫持、配置被篡改

**CVE相似度**: CWE-352 (CSRF)

#### 8. Session固定攻击
**位置**: `web/controllers/login.go:62-66`
**危害**:
- 登录时未重新生成Session ID
- Session无超时机制
- 攻击者可以固定受害者的Session

**CVE相似度**: CWE-384 (Session固定)

### 🟡 中危漏洞 (Medium)

#### 9. 登录暴力破解保护不足
**位置**: `web/controllers/login.go:48-59`
**危害**:
- 允许10次失败尝试才锁定，次数过多
- 锁定60秒后自动解除，时间过短
- 使用内存存储，服务重启后失效

**CVE相似度**: CWE-307 (暴力破解保护不当)

#### 10. 越权访问漏洞
**位置**: `web/controllers/base.go:173-207`
**危害**:
- 权限检查不够严格
- 普通用户可能访问其他用户资源
- 某些操作缺少权限验证

**CVE相似度**: CWE-639 (不恰当的授权)

#### 11. 缺少输入长度限制
**位置**: 多个控制器
**危害**:
- 未对用户输入进行长度限制
- 可能导致DoS攻击
- 大量数据可能导致内存耗尽

#### 12. 错误信息泄露
**位置**: 多处错误处理
**危害**:
- 错误信息包含系统内部信息
- 帮助攻击者了解系统结构
- 可能泄露文件路径等敏感信息

## 风险评级

| 漏洞类型 | 严重程度 | CVSS评分 | 可利用性 |
|---------|---------|---------|---------|
| 密码明文存储 | 严重 | 9.8 | 高 |
| 弱加密MD5 | 严重 | 9.1 | 高 |
| AES不安全使用 | 严重 | 8.5 | 中 |
| 认证重放攻击 | 严重 | 8.1 | 中 |
| 路径遍历 | 高危 | 7.5 | 高 |
| 时序攻击 | 高危 | 6.8 | 中 |
| CSRF | 高危 | 7.1 | 高 |
| Session固定 | 高危 | 6.5 | 中 |
| 登录暴力破解 | 中危 | 5.3 | 高 |
| 越权访问 | 中危 | 6.1 | 中 |

## 修复建议

### 立即修复 (P0)
1. ✅ 使用bcrypt/argon2替代MD5存储密码
2. ✅ 修复AES加密，使用随机IV
3. ✅ 修复路径遍历漏洞，添加路径验证
4. ✅ 使用常量时间比较防止时序攻击

### 高优先级 (P1)
5. ✅ 添加CSRF Token保护
6. ✅ 实现Session安全管理
7. ✅ 减小认证时间窗口到5秒
8. ✅ 加强登录保护：3次失败锁定30分钟

### 中优先级 (P2)
9. ✅ 加强权限验证
10. ✅ 添加输入长度限制
11. ✅ 优化错误信息处理

## 攻击场景示例

### 场景1: 数据库文件泄露导致全系统沦陷
攻击者通过漏洞获取`conf/clients.json`文件，由于密码明文存储，攻击者可以直接：
1. 登录所有用户账户
2. 获取所有客户端VerifyKey
3. 接管所有内网穿透隧道
4. 访问所有内网服务

### 场景2: 路径遍历读取敏感文件
```bash
curl http://nps-server/../../../../../../etc/passwd
curl http://nps-server/../conf/nps.conf
```

### 场景3: CSRF劫持管理员
攻击者构造恶意页面，诱导管理员访问：
```html
<form action="http://nps-server/client/add" method="POST">
  <input name="vkey" value="hacker_key">
  <!-- 创建后门客户端 -->
</form>
<script>document.forms[0].submit()</script>
```

## 总体安全评分
**当前安全评分: 3.2/10 (高风险)**

修复所有严重和高危漏洞后预期评分: **7.5/10 (中等风险)**

## 审计工具
- 人工代码审查
- 静态代码分析
- 安全最佳实践对比

## 联系信息
如有疑问，请联系安全团队。
